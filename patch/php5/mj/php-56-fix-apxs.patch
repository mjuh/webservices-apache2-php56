commit 3001d31297c0e9c99d47e2f154b51d58925f75e1
Author: Oleg Pykhalov <go.wigust@gmail.com>
Date:   Tue Jul 30 17:15:30 2019 +0300

    apxs

diff --git a/configure b/configure
index 6951f63..71f146a 100755
--- a/configure
+++ b/configure
@@ -6357,7 +6357,7 @@ IFS="- /.
 
   case $host_alias in
   *aix*)
-    APXS_LIBEXECDIR=`$APXS -q LIBEXECDIR`
+    APXS_LIBEXECDIR="$prefix/modules"
     EXTRA_LDFLAGS="$EXTRA_LDFLAGS -Wl,-brtl -Wl,-bI:$APXS_LIBEXECDIR/httpd.exp"
     PHP_AIX_LDFLAGS="-Wl,-brtl"
     build_type=shared
@@ -6541,7 +6541,7 @@ EOF
   if test "$?" != "0"; then
     APACHE_INSTALL="$APXS -i -a -n php5 $SAPI_SHARED" # Old apxs does not have -S option
   else
-    APXS_LIBEXECDIR='$(INSTALL_ROOT)'`$APXS -q LIBEXECDIR`
+    APXS_LIBEXECDIR="$prefix/modules"
     if test -z `$APXS -q SYSCONFDIR`; then
       APACHE_INSTALL="\$(mkinstalldirs) '$APXS_LIBEXECDIR' && \
                        $APXS -S LIBEXECDIR='$APXS_LIBEXECDIR' \
@@ -7635,7 +7635,7 @@ IFS="- /.
     as_fn_error $? "Please note that Apache version >= 2.0.40 is required" "$LINENO" 5
   fi
 
-  APXS_LIBEXECDIR='$(INSTALL_ROOT)'`$APXS -q LIBEXECDIR`
+  APXS_LIBEXECDIR="$prefix/modules"
   if test -z `$APXS -q SYSCONFDIR`; then
     INSTALL_IT="\$(mkinstalldirs) '$APXS_LIBEXECDIR' && \
                  $APXS -S LIBEXECDIR='$APXS_LIBEXECDIR' \
@@ -8459,7 +8459,7 @@ IFS="- /.
     as_fn_error $? "Please note that Apache version >= 2.0.44 is required" "$LINENO" 5
   fi
 
-  APXS_LIBEXECDIR='$(INSTALL_ROOT)'`$APXS -q LIBEXECDIR`
+  APXS_LIBEXECDIR="$prefix/modules"
   if test -z `$APXS -q SYSCONFDIR`; then
     INSTALL_IT="\$(mkinstalldirs) '$APXS_LIBEXECDIR' && \
                  $APXS -S LIBEXECDIR='$APXS_LIBEXECDIR' \
@@ -9270,7 +9270,7 @@ IFS="- /.
 
   case $host_alias in
   *aix*)
-    APXS_LIBEXECDIR=`$APXS -q LIBEXECDIR`
+    APXS_LIBEXECDIR="$prefix/modules"
     EXTRA_LDFLAGS="$EXTRA_LDFLAGS -Wl,-brtl -Wl,-bI:$APXS_LIBEXECDIR/httpd.exp"
     PHP_AIX_LDFLAGS="-Wl,-brtl"
     build_type=shared
@@ -9454,7 +9454,7 @@ EOF
   if test "$?" != "0"; then
     APACHE_HOOKS_INSTALL="$APXS -i -a -n php5 $SAPI_SHARED" # Old apxs does not have -S option
   else
-    APXS_LIBEXECDIR='$(INSTALL_ROOT)'`$APXS -q LIBEXECDIR`
+    APXS_LIBEXECDIR="$prefix/modules"
     if test -z `$APXS -q SYSCONFDIR`; then
       APACHE_HOOKS_INSTALL="\$(mkinstalldirs) '$APXS_LIBEXECDIR' && \
                        $APXS -S LIBEXECDIR='$APXS_LIBEXECDIR' \
diff --git a/sapi/apache2handler/config.m4 b/sapi/apache2handler/config.m4
index 02f8a0b..1f965a7 100644
--- a/sapi/apache2handler/config.m4
+++ b/sapi/apache2handler/config.m4
@@ -66,7 +66,7 @@ if test "$PHP_APXS2" != "no"; then
     AC_MSG_ERROR([Please note that Apache version >= 2.0.44 is required])
   fi
 
-  APXS_LIBEXECDIR='$(INSTALL_ROOT)'`$APXS -q LIBEXECDIR`
+  APXS_LIBEXECDIR="$prefix/modules"
   if test -z `$APXS -q SYSCONFDIR`; then
     INSTALL_IT="\$(mkinstalldirs) '$APXS_LIBEXECDIR' && \
                  $APXS -S LIBEXECDIR='$APXS_LIBEXECDIR' \
